# Development runtime

Phase 1 không cần runtime binaries. Phase 2 đặt artifact theo target ở đây, với explicit version/checksum manifest. Các binary bỏ qua Git; README được giữ. Không trỏ production runtime vào LocalWP hoặc executable global.

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only bundled shell can read and save application configuration.",
  "windows": ["main"],
  "permissions": ["allow-get-shell-info", "allow-save-store-name"]
}
